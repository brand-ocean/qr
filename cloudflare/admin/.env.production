# Production Convex deployment — used by `vite build` (npm run deploy).
# Local dev uses admin/.env.local (dev deployment giant-eagle-381).
VITE_CONVEX_URL=https://tacit-crab-381.eu-west-1.convex.cloud
